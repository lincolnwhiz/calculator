Servicing Calculator — React export
This archive contains:
- Vite React skeleton (package.json, vite.config.js)
- src/components/ServicingCalculator.jsx (loads JSON files exported from the workbook)
- src/data/ (one JSON file per sheet exported from the uploaded workbook)
- README with quick start instructions

To run locally:
1. unzip servicing-react-app.zip
2. cd servicing_react_export
3. npm install
4. npm run dev
